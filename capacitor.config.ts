import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'capacitor-plugins-issue-703',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
